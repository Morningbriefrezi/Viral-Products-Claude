name: 🌍 World Viral Product Hunter

on:
  schedule:
    - cron: '0 5 * * *'

  workflow_dispatch:
    inputs:
      force_run:
        description: 'Force run even if already ran today'
        required: false
        default: 'false'
        type: boolean

permissions:
  contents: write

env:
  OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
  TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
  TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
  MAX_DAYS: 15
  PRODUCTS_PER_DAY: 10

defaults:
  run:
    working-directory: viral-product-hunter

jobs:
  hunt-products:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: 📥 Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: 📦 Install dependencies
        run: npm install --no-package-lock

      - name: 📂 Restore data files
        run: |
          if [ -f data/history.json ]; then
            cp data/history.json history.json
            echo "✅ Restored history.json"
          else
            echo '{"products":[]}' > history.json
            echo "🆕 Created fresh history.json"
          fi

          if [ -f data/state.json ]; then
            cp data/state.json state.json
            echo "✅ Restored state.json"
          else
            echo '{"currentDay":0,"lastRunDate":null}' > state.json
            echo "🆕 Created fresh state.json"
          fi

      - name: 🔄 Force run override
        if: ${{ github.event.inputs.force_run == 'true' }}
        run: |
          node -e "
            const fs = require('fs');
            const state = JSON.parse(fs.readFileSync('state.json','utf-8'));
            state.lastRunDate = null;
            fs.writeFileSync('state.json', JSON.stringify(state, null, 2));
            console.log('✅ Force run: reset lastRunDate');
          "

      - name: 🚀 Run Viral Product Hunter
        run: node src/run-once.js

      - name: 💾 Save data to repo
        run: |
          mkdir -p data
          cp history.json data/history.json
          cp state.json data/state.json

          git config user.name "Viral Product Hunter Bot"
          git config user.email "bot@github-actions"
          git add data/history.json data/state.json

          if git diff --cached --quiet; then
            echo "📭 No changes to commit"
          else
            CURRENT_DAY=$(node -e "const s=JSON.parse(require('fs').readFileSync('state.json','utf-8'));console.log(s.currentDay)")
            git commit -m "🤖 Day ${CURRENT_DAY} — $(date -u +%Y-%m-%d) product hunt complete"
            git push
            echo "✅ Data pushed"
          fi

      - name: 🏁 Check completion
        run: |
          node -e "
            const s = JSON.parse(require('fs').readFileSync('state.json','utf-8'));
            console.log('📊 Day ' + s.currentDay + '/15');
            if (s.currentDay >= 15) console.log('🏁 All 15 days completed!');
            else console.log('⏰ Next run: tomorrow at 05:00 UTC');
          "
